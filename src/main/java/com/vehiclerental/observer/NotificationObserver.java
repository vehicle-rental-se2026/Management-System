package com.vehiclerental.observer;
/**
 * The NotificationObserver interface defines the
 * contract for receiving notification updates.
 */
public interface NotificationObserver {

    void update(String message);

}